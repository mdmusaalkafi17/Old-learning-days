#include<stdio.h>

int main()
{
    int numbers[10] = {10, 20, 30, 40, 50};

    printf("First element of array is: %d\n", numbers[0]);
    printf("Third element of array is: %d\n", numbers[2]);

    return 0;
}
